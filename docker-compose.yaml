services:
  backend:
    image: python:3.11-slim
    container_name: exchange-rate-backend
    working_dir: /workspace/backend
    volumes:
      - .:/workspace
    ports:
      - "8000:8000"
    env_file:
      - backend/.env
    tty: true
    stdin_open: true
    command: bash
